export default function DragonIcon({ className = 'w-8 h-8' }) {
  return (
    <img
      src="/chimueloylanovia.png"
      alt="Chimuelo y su novia"
      className={`object-contain shrink-0 ${className}`}
    />
  );
}
