POSTGRES_USER=bookofdate
POSTGRES_PASSWORD=bookofdate
POSTGRES_DB=bookofdate
JWT_SECRET=replace-with-a-long-random-string

# Gmail SMTP para notificaciones por correo (opcional; si se omite, solo se loguea un aviso)
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
FRONTEND_URL=http://localhost:8080
